# Dependencies / virtualenvs
node_modules/
.venv/
venv/

# Python
__pycache__/
*.py[cod]
*.egg-info/

# Build output
dist/
.vite/
*.tsbuildinfo
ResearchSense/frontend/vite.config.js
ResearchSense/frontend/vite.config.d.ts

# Env
.env
.env.local

# Editor / tooling
.playwright-mcp/
.DS_Store

# The structured RAG index (~30 MB since the full Bahria import, no paper
# full-text) is COMMITTED so deploys don't have to rebuild it: the rebuild peaks
# around 1.6 GB of RAM, far above the free tier's limit. Rebuild + commit it when
# the JSON corpus changes, using the fact-card refresh that preserves full text:
#   python -c "from scripts.build_index import rebuild_preserving_fulltext as r; r()"
# NOTE: a local build that includes downloaded paper PDFs produces a much larger
# index — do NOT commit that one; keep the committed index to the structured build.

# Runtime cache of admin-approved faculty submissions (preserved across index
# rebuilds). Written at runtime like the DB/staged dir — environment-specific,
# never committed, so it can't leak local/test submissions into the repo.
ResearchSense/backend/app/data/submitted_publications.json

# Data belonging to institutions that signed up for their own workspace. Created
# at runtime and private to each deployment, so it is never committed.
ResearchSense/backend/app/data/workspaces/

# Downloaded paper PDFs (1.1 GB) and coverage artifacts
ResearchSense/backend/papers/*.pdf
ResearchSense/backend/.coverage
